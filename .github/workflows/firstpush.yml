name: First push

on: 
  push: 

jobs:
  Tarea1:
    runs-on: ubuntu-22.04

    steps:
      - name: say ayy
        run: |
          echo "Hi my first Push"
          echo "My name is Carlos Palma"
          echo "Mensaje: ${{ github.event.head_commit.message }}"
          echo "Autor: ${{ github.event.head_commit.author.name }}"
          echo "ID Commit: ${{ github.sha }}"
          echo "URL Commit: https://github.com/${{ github.repository }}/commit/${{ github.sha }}"
          echo "Rama: ${{ github.ref_name }}"
          echo "Repositorio: ${{ github.repository }}"
